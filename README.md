
# 📚 ระบบจัดการคู่มือการใช้งาน HDC (MkDocs)

โปรเจกต์นี้ใช้สำหรับรวบรวมและแสดงผลคู่มือการใช้งานระบบ HDC (Health Data Center) ในรูปแบบเว็บไซต์เอกสารที่สวยงามและใช้งานง่าย โดยใช้เครื่องมือ **MkDocs** ร่วมกับ **Material Theme**

---

## 🛠 การเตรียมความพร้อม (Prerequisites)

ก่อนเริ่มใช้งาน คุณจำเป็นต้องติดตั้งโปรแกรมดังต่อไปนี้:

* [Python 3.x](https://www.python.org/)
* [pip](https://pip.pypa.io/en/stable/installation/) (ตัวจัดการแพ็กเกจของ Python)

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์

### 1. Clone โปรเจกต์
```bash
git clone https://github.com/dmdmoph/hdc-docs.git
```

### 2. เข้าไปยังโฟลเดอร์โปรเจกต์
ติดตั้ง MkDocs และ Extension ที่จำเป็น (Admonitions, Material Theme):
```bash
cd hdc-docs
```

### 3. ติดตั้ง Dependencies
ติดตั้ง MkDocs และ Extension ที่จำเป็น (Admonitions, Material Theme):
```bash
pip install mkdocs-material
```

### 4. รันเพื่อดูผลลัพธ์ (Local Development)
ใช้คำสั่งนี้เพื่อเปิดเว็บเซิร์ฟเวอร์จำลองในเครื่อง:
```bash
mkdocs serve
```

จากนั้นเปิด Browser ไปที่: **[http://127.0.0.1:8000](http://127.0.0.1:8000)** 

---

## 📂 โครงสร้างโฟลเดอร์

* `docs/`: เก็บไฟล์เนื้อหาคู่มือทั้งหมด (`.md`)
    * `admin`: เก็บไฟล์คู่มือสำหรับแอดมิน สสจ.
    * `manual`: เก็บไฟล์คู่มือสำหรับผู้ใช้งานทั่วไป
    * `assets/`: เก็บไฟล์รูปภาพประกอบคู่มือ
* `mkdocs.yml`: ไฟล์ตั้งค่าหลักของระบบ (เมนู, สี, Extensions)

---

## 📝 วิธีการเพิ่มเนื้อหาใหม่
1. สร้างไฟล์ `.md` ใหม่ในโฟลเดอร์ `docs/xxx/`
2. เพิ่มหัวข้อและชื่อไฟล์ลงในส่วน `nav:` ในไฟล์ `mkdocs.yml`
3. บันทึกไฟล์ ระบบจะทำการ Refresh หน้าเว็บให้โดยอัตโนมัติ

---

## ✨ ฟีเจอร์ที่เปิดใช้งาน
* **✅ Admonition:** กล่องข้อความแจ้งเตือน (Warning, Info, Success)
* **✅ Search**: ระบบค้นหาภายในคู่มือ
* **✅ Responsive**: รองรับการแสดงผลบนมือถือและแท็บเล็ต

---

## 🌐 การ Deploy ขึ้น GitHub Pages (github.io)

เมื่อคุณแก้ไขเนื้อหาเสร็จแล้ว และต้องการอัปเดตเว็บไซต์ที่ออนไลน์อยู่ สามารถใช้คำสั่งนี้เพื่อ Build และ Deploy ได้ทันที:

### 1. ตรวจสอบสถานะไฟล์
```bash
git add .
```
```bash
git commit -m "Update manual content"
```

### 2. ส่งไฟล์ต้นฉบับขึ้น GitHub
```bash
git push origin main
```

### 3. สั่ง Deploy ขึ้น GitHub Pages (สร้าง branch gh-pages อัตโนมัติ)
```bash
mkdocs gh-deploy
```
**หมายเหตุ:** หลังจากรันคำสั่ง `mkdocs gh-deploy` สำเร็จ เว็บไซต์จะออนไลน์ที่: [https://dmdmoph.github.io/hdc-docs](https://dmdmoph.github.io/hdc-docs)

---

### ⚙️ สิ่งที่ต้องตั้งค่าเพิ่มเติม (เพื่อให้ Deploy สำเร็จ)

เพื่อให้คำสั่ง `mkdocs gh-deploy` ทำงานได้และแสดงผลได้ถูกต้อง ต้องตรวจสอบ 2 จุดนี้:

#### 1. แก้ไขไฟล์ `mkdocs.yml`
ต้องเพิ่ม `site_url` เพื่อให้ระบบรู้ที่อยู่ของเว็บไซต์:
```bash
site_url: https://dmdmoph.github.io/hdc-docs/
```

#### 2. ตั้งค่าที่ GitHub Repositor
1. ไปที่หน้า Repository บน GitHub
2. คลิกที่เมนู **Settings** > **Pages**
3. ในส่วน **Build and deployment** > **Branch** ให้เลือกเป็น `gh-pages` และโฟลเดอร์ `/ (root)`
4. กด **Save**

---

## 👨‍💻 ผู้จัดทำ
* **หน่วยงาน:** กลุ่มพัฒนาการบริหารข้อมูล ศูนย์เทคโนโลยีสารสนเทศและการสื่อสาร สำนักงานปลัดกระทรวงสาธารณสุข
* **เว็บไซต์:** [https://dmd-ict.moph.go.th](https://dmd-ict.moph.go.th)